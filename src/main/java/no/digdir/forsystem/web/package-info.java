/**
 * Web layer: Thymeleaf controllers, the {@code /api} REST surface, and Spring Security config.
 * The only module that derives a default billing period from the clock (as a UI suggestion only).
 * Depends on {@code billing}, {@code registry}, {@code usage}, {@code export}, {@code common}.
 */
package no.digdir.forsystem.web;
