/**
 * Cross-cutting building blocks: the {@code Periode} value type, money helpers,
 * audit logging, and error handling. Depended on by every other module; depends on none.
 */
package no.digdir.forsystem.common;
